# admatix-verifier

The AdMatix independent verification engine — a FastAPI service that grades
the causal effect of executed ad actions against an `H0` packet, and emits a
deterministic guardrail-compliance proof alongside.

See `docs/runbooks/verifier.md` for install + boot instructions and
`docs/build/WP-R-verifier.md` for the build spec.
