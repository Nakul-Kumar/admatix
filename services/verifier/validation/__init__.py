"""Method-validation harness for the verifier's bespoke causal estimators.

This package is *not* part of the runtime API; it is the audit harness that
proves the BSTS (statsmodels) and OPE (numpy) implementations are
statistically correct. See `docs/phase-reports/verifier-method-validation.md`.
"""
