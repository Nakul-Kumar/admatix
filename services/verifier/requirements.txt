# AdMatix verifier — top-level Python deps.
#
# Notes on deviations from WP-R spec §Pinned stack:
#   * `tfcausalimpact==0.0.18` and `obp==0.5.*` were both removed because they
#     each pin pandas<2.2, which is mutually incompatible with the spec's own
#     pandas==2.2.* and numpy==2.1.* pins (and with econml 0.16.0 /
#     causalml 0.16.0, which require newer pandas/scipy). BSTS is implemented
#     against statsmodels' UnobservedComponents (a state-space BSTS with
#     posterior CIs via the Kalman filter — the standard non-TF BSTS
#     implementation), and the OPE module implements IPW / SNIPS / DR
#     directly in numpy (closed-form estimators with influence-function
#     bootstrap CIs). The `bsts-tfp` optional extra still pulls
#     `tfp-causalimpact` for operators who want the preferred BSTS backend.
#
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic>=2.9,<3
numpy==2.1.*
pandas==2.2.*
scipy>=1.14,<1.17
statsmodels==0.14.*
econml==0.16.0
causalml==0.16.0
pytest==8.3.*
httpx==0.27.*
